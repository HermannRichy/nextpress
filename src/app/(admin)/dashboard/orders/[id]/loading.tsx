import { EditorLoader } from "@/components/admin/dashboard/page-loader";

// Détail de commande : même gabarit deux colonnes que l'éditeur.
export default function Loading() {
    return <EditorLoader />;
}
