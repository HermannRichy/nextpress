import { PostsLoader } from "@/components/admin/dashboard/page-loader";

// Même structure que la page Posts : en-tête, filtres, table.
export default function Loading() {
    return <PostsLoader />;
}
