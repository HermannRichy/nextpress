import { SettingsLoader } from "@/components/admin/dashboard/page-loader";

// Même structure que Réglages : en-tête, barre d'onglets, contenu.
export default function Loading() {
    return <SettingsLoader />;
}
